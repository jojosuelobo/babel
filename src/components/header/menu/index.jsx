/*import './menu.sass';

const Menu = ({ isOpen, onClose }) => {
  return (
    <div className={`menu${isOpen ? 'open' : ''}`}>
      <ul>
        <li> Configurações </li>
        <li> Sair </li>
      </ul>
      <button onClick={onClose}>Fechar</button>
    </div>
  )
};

export default Menu;

*/